<UserControl
    x:Class="Fluence.Wpf.Demo.Pages.GalleryWindowPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="clr-namespace:Fluence.Wpf.Controls;assembly=Fluence.Wpf"
    xmlns:uicore="clr-namespace:Fluence.Wpf;assembly=Fluence.Wpf"
    d:DesignHeight="900"
    d:DesignWidth="900"
    mc:Ignorable="d">
    <ui:SmoothScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24" HorizontalAlignment="Stretch">
            <TextBlock
                Margin="0,0,0,4"
                ui:TextBlockExtensions.Typography="Title"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                Text="Window" />
            <TextBlock
                Margin="0,0,0,24"
                Style="{StaticResource SectionDescriptionStyle}"
                Text="These examples configure the demo FluenceWindow at runtime: theme, accent color, backdrop material, caption buttons, title visibility, and title bar chrome." />

            <TextBlock
                Margin="0,0,0,4"
                Style="{StaticResource SectionHeaderStyle}"
                Text="Theme and accent" />
            <TextBlock
                Margin="0,0,0,8"
                Style="{StaticResource SectionDescriptionStyle}"
                Text="Switch application theme, opt in to system theme watching, or apply a custom accent swatch." />
            <ui:Card
                Margin="0,0,0,16"
                Padding="16"
                Variant="{x:Static uicore:CardVariant.Default}">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock
                                Margin="0,0,0,8"
                                FontWeight="SemiBold"
                                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                Text="Theme" />
                            <WrapPanel Margin="0,0,0,8">
                                <ui:RadioButton
                                    x:Name="ThemeLight"
                                    Margin="0,0,12,8"
                                    Checked="ThemeRadioButton_Checked"
                                    Content="Light"
                                    GroupName="ThemeGroup" />
                                <ui:RadioButton
                                    x:Name="ThemeDark"
                                    Margin="0,0,12,8"
                                    Checked="ThemeRadioButton_Checked"
                                    Content="Dark"
                                    GroupName="ThemeGroup" />
                                <ui:RadioButton
                                    x:Name="ThemeHighContrast"
                                    Margin="0,0,12,8"
                                    Checked="ThemeRadioButton_Checked"
                                    Content="HighContrast"
                                    GroupName="ThemeGroup" />
                                <ui:RadioButton
                                    x:Name="ThemeAuto"
                                    Margin="0,0,12,8"
                                    Checked="ThemeRadioButton_Checked"
                                    Content="Auto"
                                    GroupName="ThemeGroup"
                                    IsChecked="True" />
                            </WrapPanel>
                            <StackPanel Margin="0,0,0,8" Orientation="Horizontal">
                                <ui:ToggleSwitch
                                    x:Name="ThemeWatcherToggle"
                                    Margin="0,0,10,0"
                                    Checked="SystemThemeWatcher_Toggled"
                                    IsChecked="True"
                                    Unchecked="SystemThemeWatcher_Toggled" />
                                <TextBlock
                                    x:Name="SystemThemeLabel"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="Watching: Yes" />
                            </StackPanel>
                            <TextBlock
                                x:Name="ThemeStateLabel"
                                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                Text="Current: Auto" />
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock
                                Margin="0,0,0,8"
                                FontWeight="SemiBold"
                                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                Text="Accent color" />
                            <WrapPanel Margin="0,0,0,8">
                                <ui:Button
                                    Margin="0,0,8,8"
                                    Background="#EF5858"
                                    Click="AccentSwatch_Click"
                                    Style="{StaticResource AccentSwatchStyle}"
                                    Tag="#EF5858" />
                                <ui:Button
                                    Margin="0,0,8,8"
                                    Background="#F19263"
                                    Click="AccentSwatch_Click"
                                    Style="{StaticResource AccentSwatchStyle}"
                                    Tag="#F19263" />
                                <ui:Button
                                    Margin="0,0,8,8"
                                    Background="#F6D65A"
                                    Click="AccentSwatch_Click"
                                    Style="{StaticResource AccentSwatchStyle}"
                                    Tag="#F6D65A" />
                                <ui:Button
                                    Margin="0,0,8,8"
                                    Background="#87C29C"
                                    Click="AccentSwatch_Click"
                                    Style="{StaticResource AccentSwatchStyle}"
                                    Tag="#87C29C" />
                                <ui:Button
                                    Margin="0,0,8,8"
                                    Background="#60A9E0"
                                    Click="AccentSwatch_Click"
                                    Style="{StaticResource AccentSwatchStyle}"
                                    Tag="#60A9E0" />
                                <ui:Button
                                    Margin="0,0,8,8"
                                    Background="#839CD8"
                                    Click="AccentSwatch_Click"
                                    Style="{StaticResource AccentSwatchStyle}"
                                    Tag="#839CD8" />
                                <ui:Button
                                    Margin="0,0,8,8"
                                    Background="#8863C8"
                                    Click="AccentSwatch_Click"
                                    Style="{StaticResource AccentSwatchStyle}"
                                    Tag="#8863C8" />
                            </WrapPanel>
                            <StackPanel Orientation="Horizontal">
                                <ui:Button
                                    MinWidth="80"
                                    Margin="0,0,10,0"
                                    Click="SystemAccent_Click"
                                    Content="System" />
                                <TextBlock
                                    Margin="0,0,6,0"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="Status:" />
                                <TextBlock
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                    Text="Accent: System" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                    <ui:HyperlinkButton
                        x:Name="ThemeAndAccentSourceLink"
                        Margin="0,12,0,0"
                        HorizontalAlignment="Right"
                        Style="{StaticResource DemoSourceLinkButtonStyle}"
                        Content="Source" />
                </StackPanel>
            </ui:Card>

            <TextBlock
                Margin="0,0,0,4"
                Style="{StaticResource SectionHeaderStyle}"
                Text="Backdrop and caption buttons" />
            <TextBlock
                Margin="0,0,0,8"
                Style="{StaticResource SectionDescriptionStyle}"
                Text="Choose the active backdrop material and set individual caption button availability." />
            <ui:Card
                Margin="0,0,0,16"
                Padding="16"
                Variant="{x:Static uicore:CardVariant.Default}">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="32" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock
                                Margin="0,0,0,6"
                                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                Text="Backdrop" />
                            <ui:ComboBox
                                x:Name="BackdropCombo"
                                Width="220"
                                HorizontalAlignment="Left"
                                SelectedIndex="2"
                                SelectionChanged="BackdropCombo_SelectionChanged">
                                <ComboBoxItem Content="Auto" />
                                <ComboBoxItem Content="None" />
                                <ComboBoxItem Content="Mica" />
                                <ComboBoxItem Content="Acrylic" />
                                <ComboBoxItem Content="Tabbed" />
                            </ui:ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock
                                Margin="0,0,0,6"
                                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                Text="Caption buttons" />
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="110" />
                                    <ColumnDefinition Width="14" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="110" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="8" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock
                                    Grid.Row="0"
                                    Grid.Column="0"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="Minimize" />
                                <ui:ComboBox
                                    x:Name="MinimizeVisibilityCombo"
                                    Grid.Row="0"
                                    Grid.Column="1"
                                    Width="110"
                                    SelectionChanged="CaptionVisibilityCombo_SelectionChanged">
                                    <ComboBoxItem Content="Default" IsSelected="True" />
                                    <ComboBoxItem Content="Enable" />
                                    <ComboBoxItem Content="Disable" />
                                    <ComboBoxItem Content="Hidden" />
                                    <ComboBoxItem Content="Collapsed" />
                                </ui:ComboBox>
                                <TextBlock
                                    Grid.Row="0"
                                    Grid.Column="3"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="Maximize" />
                                <ui:ComboBox
                                    x:Name="MaximizeVisibilityCombo"
                                    Grid.Row="0"
                                    Grid.Column="4"
                                    Width="110"
                                    SelectionChanged="CaptionVisibilityCombo_SelectionChanged">
                                    <ComboBoxItem Content="Default" IsSelected="True" />
                                    <ComboBoxItem Content="Enable" />
                                    <ComboBoxItem Content="Disable" />
                                    <ComboBoxItem Content="Hidden" />
                                    <ComboBoxItem Content="Collapsed" />
                                </ui:ComboBox>
                                <TextBlock
                                    Grid.Row="2"
                                    Grid.Column="0"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                    Text="Close" />
                                <ui:ComboBox
                                    x:Name="CloseVisibilityCombo"
                                    Grid.Row="2"
                                    Grid.Column="1"
                                    Width="110"
                                    SelectionChanged="CaptionVisibilityCombo_SelectionChanged">
                                    <ComboBoxItem Content="Default" IsSelected="True" />
                                    <ComboBoxItem Content="Enable" />
                                    <ComboBoxItem Content="Disable" />
                                    <ComboBoxItem Content="Hidden" />
                                    <ComboBoxItem Content="Collapsed" />
                                </ui:ComboBox>
                            </Grid>
                            <WrapPanel>
                                <ui:CheckBox
                                    x:Name="ShowWindowIconToggle"
                                    Margin="0,0,14,0"
                                    Checked="WindowChromeToggle_Changed"
                                    Content="Show Icon"
                                    IsChecked="True"
                                    Unchecked="WindowChromeToggle_Changed" />
                                <ui:CheckBox
                                    x:Name="ShowWindowTitleToggle"
                                    Checked="WindowChromeToggle_Changed"
                                    Content="Show Title"
                                    IsChecked="True"
                                    Unchecked="WindowChromeToggle_Changed" />
                            </WrapPanel>
                        </StackPanel>
                    </Grid>
                    <ui:HyperlinkButton
                        x:Name="BackdropAndCaptionButtonsSourceLink"
                        Margin="0,12,0,0"
                        HorizontalAlignment="Right"
                        Style="{StaticResource DemoSourceLinkButtonStyle}"
                        Content="Source" />
                </StackPanel>
            </ui:Card>

            <TextBlock
                Margin="0,0,0,4"
                Style="{StaticResource SectionHeaderStyle}"
                Text="Title bar chrome" />
            <TextBlock
                Margin="0,0,0,8"
                Style="{StaticResource SectionDescriptionStyle}"
                Text="Extend content into the title bar, toggle the title bar shadow, and adjust title bar height." />
            <ui:Card
                Margin="0,0,0,8"
                Padding="16"
                Variant="{x:Static uicore:CardVariant.Default}">
                <StackPanel>
                    <StackPanel Margin="0,0,0,8" Orientation="Horizontal">
                        <ui:ToggleSwitch
                            x:Name="ExtendsContentToggle"
                            Margin="0,0,10,0"
                            Checked="TitleBarToggle_Changed"
                            IsChecked="False"
                            Unchecked="TitleBarToggle_Changed" />
                        <TextBlock
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="Extends into title bar" />
                    </StackPanel>
                    <StackPanel Margin="0,0,0,8" Orientation="Horizontal">
                        <ui:ToggleSwitch
                            x:Name="HasShadowToggle"
                            Margin="0,0,10,0"
                            Checked="TitleBarToggle_Changed"
                            IsChecked="True"
                            Unchecked="TitleBarToggle_Changed" />
                        <TextBlock
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="Title bar shadow" />
                    </StackPanel>
                    <StackPanel Margin="0,0,0,8" Orientation="Horizontal">
                        <TextBlock
                            Width="60"
                            Margin="0,0,8,0"
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="Height" />
                        <ui:Slider
                            x:Name="TitleBarHeightSlider"
                            Width="180"
                            Maximum="64"
                            Minimum="32"
                            ValueChanged="TitleBarHeightSlider_Changed"
                            Value="48" />
                        <TextBlock
                            x:Name="TitleBarHeightLabel"
                            Margin="8,0,0,0"
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="48" />
                    </StackPanel>
                    <ui:HyperlinkButton
                        x:Name="TitleBarChromeSourceLink"
                        Margin="0,4,0,0"
                        HorizontalAlignment="Right"
                        Style="{StaticResource DemoSourceLinkButtonStyle}"
                        Content="Source" />
                </StackPanel>
            </ui:Card>
        </StackPanel>
    </ui:SmoothScrollViewer>
</UserControl>
