<UserControl
    x:Class="Fluence.Wpf.Demo.Pages.GalleryCategoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="clr-namespace:Fluence.Wpf.Controls;assembly=Fluence.Wpf"
    d:DesignHeight="800"
    d:DesignWidth="900"
    mc:Ignorable="d">
    <ui:SmoothScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            <TextBlock
                x:Name="CategoryPageTitle"
                Margin="0,0,0,4"
                Style="{StaticResource SectionHeaderStyle}" />
            <TextBlock
                x:Name="CategoryPageDescription"
                Margin="0,0,0,24"
                Style="{StaticResource SectionDescriptionStyle}" />
            <WrapPanel x:Name="CategoryCardsGrid" />
        </StackPanel>
    </ui:SmoothScrollViewer>
</UserControl>
