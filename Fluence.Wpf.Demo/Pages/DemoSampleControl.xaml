<UserControl
    x:Class="Fluence.Wpf.Demo.Pages.DemoSampleControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:fluence="http://schemas.fluencewpf.com"
    x:Name="Root">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border
            x:Name="SampleCard"
            Grid.Row="0"
            Padding="16"
            Background="{DynamicResource DemoControlSurfaceBrush}"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8,8,0,0"
            >
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel
                    x:Name="HeaderPanel"
                    Grid.Row="0"
                    Margin="0,0,0,12">
                    <TextBlock
                        x:Name="TitleTextBlock"
                        Margin="0,0,0,4"
                        Style="{StaticResource BodyStrongTextBlockStyle}"
                        Text="{Binding Title, ElementName=Root}" />
                    <TextBlock
                        x:Name="DescriptionTextBlock"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{Binding Description, ElementName=Root}"
                        TextWrapping="Wrap" />
                </StackPanel>

                <ContentPresenter
                    x:Name="SampleContentPresenter"
                    Grid.Row="1"
                    Content="{Binding SampleContent, ElementName=Root}" />
            </Grid>
        </Border>

        <fluence:Expander
            x:Name="SourceExpander"
            Grid.Row="1"
            Margin="0"
            Background="{DynamicResource DemoSourceSurfaceBrush}"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1,0,1,1"
            CornerRadius="0,0,8,8"
            Expanded="SourceExpander_Expanded"
            Header="Source code">
            <fluence:TabView
                x:Name="SourceTabs"
                Margin="16,8,16,16"
                IsAddTabButtonVisible="False" />
        </fluence:Expander>
    </Grid>
</UserControl>
