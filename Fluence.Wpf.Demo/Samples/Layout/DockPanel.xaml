<UserControl
    x:Class="Fluence.Wpf.Demo.Samples.Layout.DockPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:fluence="http://schemas.fluencewpf.com">
    <fluence:DockPanel
        Width="420"
        Height="220"
        Spacing="8">
        <fluence:Border
            MinHeight="56"
            Padding="14"
            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            DockPanel.Dock="Top">
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="Top" />
        </fluence:Border>
        <fluence:Border
            MinWidth="120"
            Padding="14"
            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            DockPanel.Dock="Left">
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="Left" />
        </fluence:Border>
        <fluence:Border
            Padding="14"
            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="Fill" />
        </fluence:Border>
    </fluence:DockPanel>
</UserControl>
