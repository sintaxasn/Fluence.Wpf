# Fluence.Wpf.Gallery

This folder is currently reserved and contains no buildable project. The active visual gallery lives in [Fluence.Wpf.Demo](../Fluence.Wpf.Demo/README.md).

## Intended Use

Use this folder only if the repository later splits the gallery into a separate app or packaging surface. Until then, do not add consumer guidance here that differs from `Fluence.Wpf.Demo`; keep the demo README and root documentation as the source of truth.

## Maintenance Notes

If this folder becomes active, add a project file, update `Fluence.Wpf.sln`, add tests or run instructions, and replace this placeholder README with component-specific guidance.
